# Set the discovered Rover URL and the same token as private secrets.h.
ROVER_BASE_URL=http://192.0.2.1
ROVER_API_TOKEN=replace-with-a-long-random-token
# VBB_BRIDGE_TOKEN is generated per launch, not saved here.
